# Referências

> Fonte única. Formato **machine-readable** — `scripts/gate.py` faz o parse. Não altere a
> estrutura dos blocos. Regras completas em `.claude/rules/fontes-e-citacoes.md`.
>
> **O campo `origem` é obrigatório** e diz *como sabemos que esta referência existe*.
> Entrada sem origem verificável é tratada como inventada e bloqueia o gate.

Chave = `[SOBRENOME-DO-PRIMEIRO-AUTOR, ANO]`. Mesmo autor e ano → sufixo `a`, `b`, `c`.

---

<!-- MODELO — copie, não apague:
